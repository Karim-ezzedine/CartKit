import XCTest
@testable import CartKitCore

final class CartKitCoreTests: XCTestCase {

    func test_placeholder() {
        // Just to ensure the test target is wired correctly
        XCTAssertNotNil(CartKitCoreMarker.self)
    }
}

